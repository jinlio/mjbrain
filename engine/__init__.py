"""engine — 规则心脏：重演牌局，产出合法动作集与确定性特征。

职责（全部确定性代码，模型不参与）：
- 维护 mjai 事件流 -> 牌局状态
- 枚举当前决策点的合法动作子集（<=20，供 brain 层 choice 用）
- 向听/受入/点数/振听判定（HUD 直显 + 启发式兜底 B1）

选型（docs/PLAN.md M1-2）：riichienv 绑定优先，许可不符则自研枚举。
"""
